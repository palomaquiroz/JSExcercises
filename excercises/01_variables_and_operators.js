// Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma de ambos números en una 3er variable.

var numberOne = 10;
var numberTwo = 5;
var sum = numberOne + numberTwo;
console.log(sum);

// Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable.

var firstWord = 'Eso';
var secondWord = 'Tilín!';
var phrase = firstWord + ' ' + secondWord;
console.log(phrase);

// Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string)
// guardando el resultado de la suma en una 3er variable (utilizar length).


var firstPhrase = 'Hola Juan Carlos'
var secondPhrase = 'Como estás'
var phrasesLength = firstPhrase.length + secondPhrase.length;
console.log(phrasesLength);